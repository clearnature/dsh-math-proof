# 全部门禁：15 个入口（schema 自检 + 14 套回归，含架构文档漂移门禁 docs-check）
#
# 这些门禁是**纯 node 内建模块**实现的（零依赖），所以在裸 CI 里也能跑：
# 没有 dsh 安装、没有市场快照、没有 Agda 仓库时，相关断言会显式 SKIP（⏭）而不是假绿。
name: gates

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
  workflow_dispatch:

jobs:
  check-all:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['20', '22', '24']
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '${{ matrix.node }}'
      - name: 一键门禁（期望 CHECK_ALL_OK 15/15）
        run: node math-proof/scripts/check-all.mjs
